if(instance_exists(other_gem) and image_alpha != 0.2 and other_gem.image_alpha != 0.2){
	row = previous_row;
	column = previous_column;
	
	with(other_gem){
		row = previous_row;
		column = previous_column;
	}
}